# 開発ガイド

`line-bot-features` はFlaskでLINE webhookを受けるPythonボット。入口は `main.py` の `/callback`、振り分けは `line/service.py`、機能は `store/`・`ocr/`、保存は `database/`。設定の参照箇所は `const/env.py`、起動定義は `Procfile`、依存は `requirements.txt`。

## 実行・検証の条件

- 依存は分離したPython環境に用意する。`python main.py` は実際のwebhookアプリを起動するため、設定と接続先を確認してから使う。
- `main.py` の署名検証を維持する。LINEへの返信・外部API呼び出し・DB書込は実動作を伴う。リポジトリ名に `test` があるかどうかで、安全な送信先やテスト用DBだと判断しない。
- ローカルの確認は外部サービスをフェイクに置き換え、認証情報や個人のメッセージ・画像をログやテストデータに残さない。実送信・実データ更新は依頼範囲と対象を確認して行う。
- 変更したPythonは `python -m py_compile <対象.py>` で構文確認する。自動テストの実行定義は未整備なので、構文確認のみで返信・OCR・保存の動作確認済みとは扱わない。起動時importにも設定読込や外部クライアント初期化があるため、importを構文確認の代わりにしない。
- 設定値を指示ファイルへ転記せず、必要なキーと読込方法を実装の該当箇所で確認する。このリポジトリ内の定義を正とし、似た名前の別リポジトリの変更・設定を自動で流用しない。

## 作業の進め方

- `AGENTS.md` はこのファイルへの相対リンク。共通の本文は一度だけ読み、`CLAUDE.md` を編集する。
- 対象のファイル・見出し・シンボルから調べ、必要な場合だけ範囲を広げる。資料やskillsは作業に該当するものを読む。
- 不明点は質問して解消してから、その判断に依存する作業に進む。すでに決まっている事項は再確認しない。
- 文書の言語を保ち、日本語は日本人に、英語は英語圏の読者に自然に伝わる表現にする。
- 必須検証は適用条件に従って実行し、同じ差分・依存・設定・実行条件で得た結果は再利用する。問題を修正し、結果と未確認の範囲を簡潔に報告する。
- このガイドには継続して必要な規約と参照先を残す。進捗や設定値、他の資料・skillsの手順は複製しない。
